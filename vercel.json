{
  "buildCommand": "npm install && npm --prefix ./api install && npm --prefix ./frontend install && npm --prefix ./frontend run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install && npm --prefix ./api install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "VITE_API_URL": "/api",
    "DB_PATH": "/tmp/pgds.db",
    "JWT_SECRET": "@jwt_secret_prod",
    "FRONTEND_URL": "@frontend_url_prod",
    "NODE_ENV": "production"
  }
}
